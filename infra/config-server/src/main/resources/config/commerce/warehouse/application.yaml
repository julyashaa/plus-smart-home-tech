server:
  port: 0

spring:
  application:
    name: warehouse

  datasource:
    url: jdbc:postgresql://localhost:5435/warehouse
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info