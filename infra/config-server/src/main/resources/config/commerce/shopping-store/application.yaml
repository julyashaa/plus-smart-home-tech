server:
  port: 0

spring:
  application:
    name: shopping-store

  datasource:
    url: jdbc:postgresql://localhost:5433/shopping-store
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info