server:
  port: 8080

grpc:
  server:
    port: 59091

spring:
  application:
    name: collector

  kafka:
    bootstrap-servers: localhost:9092

collector:
  kafka:
    topics:
      sensors: telemetry.sensors.v1
      hubs: telemetry.hubs.v1